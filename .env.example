# AI Coach 环境变量模板
# 复制本文件为 .env.local 后填入真实值。.env.local 已被 .gitignore 屏蔽。

# ─── 主 AI 服务（用于 parseProblem / 代码分析 / 问 AI 等所有 LLM 调用） ───
# provider: 任意标识，目前只用作日志，建议 minimax / openai / deepseek 等
AI_COACH_PROVIDER=minimax
# OpenAI 兼容 API 的 baseUrl（注意末尾不要 /v1）
AI_COACH_BASE_URL=https://api.minimax.chat/v1
# API key（请从对应平台获取）
AI_COACH_KEY=your-api-key-here
# 模型名（minimax 推荐 MiniMax-M2，OpenAI 用 gpt-4o-mini，DeepSeek 用 deepseek-chat 等）
AI_COACH_MODEL=MiniMax-M2

# ─── 后端 OCR / 视觉服务（仅 server 用，处理 OJ 题目里的图片） ───
# Ollama base URL（默认本机）
AICC_OLLAMA_BASE=http://127.0.0.1:11434
# 视觉模型（推荐 sam:latest 或 minicpm-v）
AICC_VISION_MODEL=sam:latest
